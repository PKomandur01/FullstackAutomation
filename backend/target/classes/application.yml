spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/automationdb
    username: postgres
    password: 

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
